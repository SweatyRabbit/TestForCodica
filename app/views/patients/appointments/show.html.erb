<h1> <%= t('appointments.details') %> </h1>
<table class="border-collapse table-fixed w-full text-sm">
  <tbody>
    <tr>
      <td>
        <%= t('appointments.doctors_name') %>
      </td>
      <td>
        <%= @appointment.doctor.user.full_name %>
      </td>
    </tr>
    <tr>
      <td>
        <%= t('appointments.status') %>
      </td>
      <td>
        <%= @appointment.status %>
      </td>
    </tr>
    <tr>
      <td>
        <%= t('appointments.date') %>
      </td>
      <td>
        <%= @appointment.date %>
      </td>
    </tr>
    <tr>
      <td>
        <%= t('appointments.recommendation') %>
      </td>
      <td>
        <%= @appointment.recommendation %>
      </td>
    </tr>
  </tbody>
</table>

<button class="px-4 py-2 font-semibold text-sm bg-cyan-500 text-white rounded-full shadow-sm">
  <%= link_to t('navigation.home_page'), root_path %>
</button>
